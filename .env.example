# Copiar este archivo a .env y rellenar con tus propias claves

# OpenAI API Key (para GPT y embeddings)
OPENAI_API_KEY=sk-...

# Anthropic API Key (para Claude)
ANTHROPIC_API_KEY=sk-ant-...

# Proveedor LLM principal (openai o anthropic)
LLM_PROVIDER=openai

# Proveedor de embeddings (openai)
EMBEDDING_PROVIDER=openai

# PostgreSQL (para desarrollo local sin Docker)
DATABASE_URL=jdbc:postgresql://localhost:5432/luisamigo
DATABASE_USERNAME=luisamigo
DATABASE_PASSWORD=luisamigo_dev_password
